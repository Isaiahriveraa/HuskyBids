'use client';

/**
 * SSO Callback Page
 * Handles OAuth redirects from social login providers (Google, etc.)
 * Global loading.jsx handles the loading screen during authentication process
 */
export default function SSOCallback() {
  // Global loading.jsx handles the loading state
  return null;
}
